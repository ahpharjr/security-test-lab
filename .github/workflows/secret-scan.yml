# name: Gitleaks Secret Scan

# on:
#   push:
#   pull_request:

# jobs:
#   scan:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Install Gitleaks
#         run: |
#           curl -sSL https://github.com/gitleaks/gitleaks/releases/latest/download/gitleaks-linux-amd64 -o gitleaks
#           chmod +x gitleaks

#       - name: Run Gitleaks (Generate SARIF Report)
#         run: |
#           ./gitleaks detect \
#             --source . \
#             --report-format sarif \
#             --report-path gitleaks-report.sarif \
#             --no-banner
#         continue-on-error: true

#       - name: Upload SARIF Report to GitHub Security Dashboard
#         uses: github/codeql-action/upload-sarif@v2
#         with:
#           sarif_file: gitleaks-report.sarif


name: Gitleaks Secret Scan

on:
  push:
  pull_request:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Gitleaks (SARIF output)
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --source . --report-format sarif --report-path gitleaks-report.sarif --no-banner
        continue-on-error: true

      - name: Debug - list files
        run: ls -lah

      - name: Upload SARIF report
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gitleaks-report.sarif

