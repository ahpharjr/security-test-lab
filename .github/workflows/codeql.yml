# name: CodeQL
# on: [push, pull_request]
# jobs:
#   analyze:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       security-events: write
#     steps:
#       - uses: actions/checkout@v4
#       - uses: github/codeql-action/init@v3
#         with:
#           languages: javascript
#       - uses: github/codeql-action/analyze@v3
name: CodeQL

on: [push, pull_request]

permissions:
  contents: read
  actions: read          # <--- required so CodeQL can call workflow-run endpoints
  security-events: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v3
        # analyze@v3 will upload sarif automatically; no upload-sarif step required
